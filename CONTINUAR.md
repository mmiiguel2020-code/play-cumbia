# Continuar PICHADAW

Producto: `PICHADAW.txt` y `README.md`. Instalar copias: `.\instalar.ps1`.

## Donde esta

| Que | Ruta |
|-----|------|
| Codigo | `Documents\play-cumbia` |
| GitHub | `mmiiguel2020-code/play-cumbia` rama `main` |
| App | `AppData\Local\Programs\Miguel Music Assistant\PICHADAW.exe` |
| Acceso | Escritorio `PICHADAW.lnk` |
| Conservado | Escritorio `PICHADAW-conservado\PICHADAW.exe` |
| VST3 | `C:\Program Files\Common Files\VST3\PICHADAW.vst3` (Synth / Instrument) |

No mezclar con `acordeon-midi` ni `tx-rx-max-2`. No `compilar.ps1 app` si se quiere conservar el exe frio.

## Hecho (comprobado en FL por Miguel)

VST3 reconocido como Synth, unico en Generators, tambien funciona como efecto.

## Git

Comprobar `git log -1`. El REC va a `Escritorio\grabaciones pichadaw` y a la lista.

## Siguiente, si Miguel pide

1. No enganchar `Vst3HostSlot` / `PichaBronco` sin que lo pida
2. UI vieja (generador, Bronco 37) solo si lo pide
